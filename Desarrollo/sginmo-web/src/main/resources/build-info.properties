# REQ-0051 — Sello de build, filtrado por Maven en tiempo de compilacion.
# Lo lee SaludService para mostrar version/fecha/commit del despliegue.
app.version=${project.version}
app.build.time=${maven.build.timestamp}
app.build.commit=${buildCommit}
